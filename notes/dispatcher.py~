from .add import add


_add_keys = ['-a', '-add', '--a', '--add']


def dispatch(arguments):
    add_key = list(_add_keys & arguments.keys())
    if add_key:
        add(arguments[add_key[0]])
    else:
        print("err")
