from .config import Config
from .store import FirebaseStore


class Note(object):

    def __init__(self):
        self.store = FirebaseStore('notes')

    def add(self, args):
        print(len(args))
        config = Config()
        print(config.get('firestore_link'))

    def get(self, args):
        print(self.store.get_all())
